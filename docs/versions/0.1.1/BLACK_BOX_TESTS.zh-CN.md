# 0.1.1 黑盒功能检查

## 需求对应关系

| 功能区域 | 状态 | 验证证据 |
| --- | --- | --- |
| 中文主界面 | 已完成 | 真实 App 窗口、控件和辅助功能树检查通过 |
| ReadyType 视觉一致性 | 已完成 | 自适应灰阶、苔绿色主色、卡片和状态层级已应用 |
| 链接校验 | 已完成 | 无协议链接显示中文错误；完整 HTTP/HTTPS 链接启用解析 |
| 格式解析 | 已完成 | 真实 App 解析本地 DASH，显示 1 个 320×180 视频格式 |
| 下载模式 | 已完成 | 最高画质、兼容 MP4、指定格式的切换与说明通过 |
| 下载与音频合并 | 已完成 | 本地真实 yt-dlp 下载和 FFmpeg/FFprobe 合并验证通过 |
| 设置与隐私 | 已完成 | 中文设置窗口、系统代理默认值、可选 Cookie、默认关闭详细日志通过 |
| 进度与取消 | 部分完成 | 状态模型和取消入口通过；长时间 UI 下载取消仍待真实网络验收 |

## 自动化证据

- `swift test --package-path apps/macos`：20 个测试、5 个测试套件通过。
- `./script/test_macos_download_integration.sh`：真实 yt-dlp 下载、FFmpeg 合并和 FFprobe 音视频轨道检查通过。
- `./script/build_and_run.sh --verify`：应用构建、打包为本地 APP、启动和进程检查通过。

## 真实环境待验收

- 已获授权的 Instagram、YouTube 等公网链接完整下载。
- 长时间公网下载的取消和临时文件行为。
- 干净 macOS 账号首次启动。
- Developer ID 签名、公证和 Gatekeeper 检查。
- 远程 GitHub CI 和 Release 工作流。

## 发布判断

本版本的本地中文 UI、格式解析和下载内核验证通过；上述真实环境项目完成前，不应宣称公开发布验收全部完成。
